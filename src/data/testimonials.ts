import { Testimonial } from "@/types";

// The spec requires real, verified testimonials only — none have been supplied yet.
// Add genuine client testimonials here before launch. The TestimonialCard section
// hides itself automatically while this array is empty.
export const testimonials: Testimonial[] = [];
